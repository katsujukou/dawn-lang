package:
  name: cli
  description: CLI common library
  dependencies:
    - ansi
    - argparse-basic
    - console
    - dodo-printer
    - prelude
    - run
  test:
    main: Test.Stella.CLI
    dependencies:
      - effect
      - spec
      - spec-node
